/**
 * @param {number[][]} image
 * @param {number} sr
 * @param {number} sc
 * @param {number} color
 * @return {number[][]}
 */
var floodFill = function(image, sr, sc, color) {
    let stack = [];
    let visited = {};

    while()

    return [[1,2,3],[4,5,6],[7,8,9]];
};